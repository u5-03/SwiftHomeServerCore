import XCTest
@testable import SwiftHomeServerCore

final class SwiftHomeServerCoreTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftHomeServerCore().text, "Hello, World!")
    }
}
